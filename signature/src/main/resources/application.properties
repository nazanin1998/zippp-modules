spring.application.name=signature
# zippp.signature.* — required for auto-configuration to bind keys.
# zippp.signature.signing-private-key=<RSA private key, PEM or Base64>
# zippp.signature.parsing-public-key=<RSA public key, PEM or Base64>